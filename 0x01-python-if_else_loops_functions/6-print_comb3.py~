#!/usr/bin/python3
for num1 in range(10):
    for num2 in range(num1 + 1, 10):
        if num1 < num2:
            print("{:02d}".format(first_digit * 10 + second_digit), end=", "
                  if first_digit * 10 + second_digit != 89 else "\n")
